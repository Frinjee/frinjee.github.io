{
  "name": "calendar-test",
  "version": "1.0.0",
  "private": true,
  "description": "Local dev server for FullCalendar work site",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve .",
    "format": "prettier --write \"**/*.{js,css,html,json}\""
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
