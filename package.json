{
  "name": "calendar-test",
  "version": "1.0.0",
  "private": true,
  "description": "Local dev server for FullCalendar work site",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve .",
    "format": "prettier --write \"**/*.{js,css,html,json}\"",

    "build:gh-root": "vite build --outDir ../trackme"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
